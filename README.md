# react-native-flux-starter-kit
React native flux starter kit

### Run Server

```
npm install
npm start
```

### Run Android

```
react-native run-android
```

### Run iOS

Open `ios/App.xcodeproj` in Xcode, Build & Run
